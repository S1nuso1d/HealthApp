# Скопируйте в keystore.properties (файл в .gitignore) и заполните.
# Генерация ключа:
#   keytool -genkey -v -keystore healthapp-upload.jks -keyalg RSA -keysize 2048 -validity 10000 -alias healthapp

storeFile=healthapp-upload.jks
storePassword=CHANGE_ME
keyAlias=healthapp
keyPassword=CHANGE_ME
